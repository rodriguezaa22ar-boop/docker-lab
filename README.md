# docker-lab

A simple Docker-based Python lab project.

## What it does
This project builds a small Python container and runs a basic application message.

## Files
- `app.py` - application entrypoint
- `Dockerfile` - container build instructions
- `.gitignore` - ignored files

## Build
```bash
docker build -t docker-lab .
